import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFF0084FF);     // Bleu WhatsApp-like
  static const Color primaryDark = Color(0xFF0066CC);
  
  static const Color background = Color(0xFF121212);  // Dark mode par défaut
  static const Color surface = Color(0xFF1E1E1E);
  
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0B0B0);
  
  static const Color success = Color(0xFF00C853);
  static const Color error = Color(0xFFFF3D00);
  static const Color warning = Color(0xFFFFC107);

  // Couleurs des messages
  static const Color messageSent = Color(0xFF0084FF);
  static const Color messageReceived = Color(0xFF2A2A2A);
}